# CV Portfolio (Flask)

## Setup

1. Create and activate a virtual environment.
2. Install dependencies:

```bash
pip install -r requirements.txt
```

## Run

```bash
python app.py
```

Open http://127.0.0.1:5000 in your browser.

## Contact submissions

Messages are stored in `data/messages.db` (SQLite) on the server.
